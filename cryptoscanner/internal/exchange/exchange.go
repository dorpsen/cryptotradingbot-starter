package exchange

// Exchange interfaces and helpers for the cryptoscanner subproject. The
// canonical interfaces are defined under `internal/exchange` at the repo root.

// This file intentionally contains no runtime logic.
