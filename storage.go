package main

// This file is intentionally minimal to avoid empty-file parse errors during builds.
// The project's main packages live under `cmd/` and `internal/`.

// No runtime code required here.
